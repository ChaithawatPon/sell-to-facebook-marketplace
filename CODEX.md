# Codex notes

The repository is intentionally small and portable.

- Edit the package in `sell-to-facebook-marketplace/`.
- Keep runtime directories untracked: `state/`, `output/`, `node_modules/`.
- Prefer package-local relative paths over machine-specific absolute paths.
- Re-run the validator and package tests before pushing.
